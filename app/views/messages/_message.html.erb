<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.nickname %></strong>
    <i><%= message.created_at.strftime("%d of %B, %Y at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
